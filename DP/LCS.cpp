#include<iostream>
#include<stdio.h>
#include<string.h>
using namespace std;
char s[250000];
char ss[250000];
int save[250000][250000];
int main(){
    scanf("%s %s",s,ss);
    memset(save,0,sizeof(save));

    for(int i=0;i<strlen(s);++i){
        
    }
}