#include <iostream>
#include <vector>

using namespace std;

int main()
{
    int n;
    cin >> n;
    int first, last, medium;
    for (int i = 0; i < n;++i){
        if(i==0)
            cin >> first;
        else if(i==n-1)
            cin >> last;
        
    }
        getchar();
    getchar();
}